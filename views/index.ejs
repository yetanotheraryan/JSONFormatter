<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JSONFormatter</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; padding: 20px; }
        textarea { width: 80%; height: 150px; margin-bottom: 10px; }
        pre { background: #f4f4f4; padding: 10px; border-radius: 5px; text-align: left; }
        .error { color: red; font-weight: bold; }
    </style>
</head>
<body>
    <h1>JSONFormatterForHoomans</h1>
    <div id="commondiv">
    <div id="Input">
      <form action="/format" method="POST">
        <h2>Unformatted JSON to be pasted below hooman</h2>
          <textarea name="jsonInput" placeholder="Paste your JSON here..."><%= input ? input : "" %></textarea><br>
          <select name="tabspace" id="tabspace">
              <option value=2>2 Tab Spaces</option>
              <option value=4 selected>4 Tab Spaces</option>
            </select>
          <button type="submit">Format JSON</button>
      </form>
    </div>
    <div id="Output">
      <% if (error) { %>
        <p class="error"><%= error %></p>
      <% } %>
          <h2>Formatted JSON for hooman</h2>
          <textarea name="jsonInput" placeholder="Paste your JSON here..."><%= formattedJson ? formattedJson : "" %></textarea><br>
    </div>
    </div>
</body>
</html>